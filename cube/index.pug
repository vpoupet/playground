doctype html
html
  head
    meta(charset="utf-8")
    link(rel="stylesheet" href="style.css")
  body
    #scene
    #buttons
      button(onclick="cube.shuffle(1000)") Shuffle
      button(onclick="cube.reset()") Reset
    h2 Instructions
    ul
      li Hover over a sticker with the mouse and use the arrow keys or Z, Q, S, D to rotate the corresponding slice
        |  in the desired direction
      li Hold the Shift key and a direction to rotate the whole cube
      li Swipe on a sticker with one finger to rotate a slice
      li Swipe on a face with two fingers to rotate the whole cube
      li Click the "Shuffle" button to shuffle the cube (1000 random moves, not animated)
      li Click the "Reset" button to restore all stickers to their original position

    script(src="gl-matrix-min.js")
    script(src="script.js")